# Surgical DevOps v2.6.0-rc.6

## Prerelease publication repair

RC.6 adds the explicit npm prerelease distribution channel `--tag next`. The immutable RC.5 tag passed Linux, macOS, and Windows qualification, but npm rejected publication before package creation because the prerelease channel was not explicit.


Português: [v2.6.0-rc.6_PT-BR.md](./v2.6.0-rc.6_PT-BR.md)

Status: **hermetic release-candidate recovery**

This candidate preserves the complete governed v2.6.0-rc.4 product scope and
corrects only release-test isolation. The immutable v2.6.0-rc.4 tag exposed
fifteen CLI, onboarding and installation tests that still depended on the
physical release checkout. No npm package or GitHub Release was published from
that failed tag.

The affected tests now execute against disposable hermetic Git repositories.
Package construction remains rooted in the real source tree, while the installed
CLI executes inside a named physical branch. Production continues to reject
detached HEAD; no operational invariant, authority boundary or fail-closed
behavior is weakened.

Qualified predecessor baseline:
`56da715284704f227675961d476e19acce6e9fa3`

Canonical workflow:
[33286652480](https://github.com/bonushora/surgical-dev-ops/actions/runs/33286652480)

```bash
npm ci
npm test
node --test tests/accelerator/npm-unified-installation.test.js
npm pack --dry-run
```

The normal checkout and a real detached-HEAD clone were qualified before the
ADR-036/ADR-037 integration. The current local integrated gateway qualification
now discovers 1206 tests: 1201 passed, zero failed and five platform-specific
tests were skipped.

## Local ADR-036/ADR-037 integrated gateway qualification

The local integrated NATURAL gateway qualification builds on the NATURAL default
checkpoint `9ed86a443da18f923b60692d7446f1fd57d0a2da` and the ADR-034
governed workspace checkpoint `f56750eba3aa07b0426f56021c072a280468ea98`, which
itself builds on the initial ADR-034 implementation commit
`2f8d9e1aa40d0d7a127e966a28e475e0f89c4bb0`.

The current implementation adds the ADR-036 persistent NATURAL governed mission
and ADR-037 structured gateway to the real runtime. NATURAL can project mission
status, live plan, changes, tests, authority, journal and governed resume while
structured gateway requests invoke workspace status, governed search, governed
read, governed diff, governed test execution and contextual approval creation
without manual copy/paste. The distinct ENGINEER proposal path and EXPERT
command path remain separate.

Local canonical suite:
1206 tests discovered; 1201 passed; zero failed and five platform-specific
tests were skipped.

This local qualification does not claim a new pushed tag, GitHub Release, npm
publication or replacement of the published native CI predecessor above. The
next publication gate still requires Ubuntu, macOS and Windows to pass on the
exact pushed commit and tag.

Ubuntu, macOS and Windows must pass on the pull request, merged main and exact
tag before npm publication. Manifest CAS, signed authorization, G1-G10
linearizable anti-replay and the incremental adversarial campaign remain
unchanged.

This is not proof of absolute security and not a completed independent audit.
